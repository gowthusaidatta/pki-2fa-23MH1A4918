version: "3.8"

services:
  app:
    build: .
    container_name: pki_2fa_app
    ports:
      - "8080:8080"
    environment:
      - TZ=UTC
    volumes:
      - seed-data:/data
      - cron-output:/cron
    restart: unless-stopped

volumes:
  seed-data:
  cron-output:
